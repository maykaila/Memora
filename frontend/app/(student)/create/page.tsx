import FlashcardCreator from "../../components/FlashcardCreator";

// We update the student page to use the new shared component
export default function StudentCreatePage() {
  return <FlashcardCreator role="student" />;
}